var check = ['რეკლამა', 'მარკეტინგი', 'ვირუსი']
var str = 'რეკლამის გამოქვეყნება'

for (var i = 0 ; i < check.length; i++) {
    if(str.search(i)) {
        alert('sheicavs akrdzalul sityvebs !')
    }
}